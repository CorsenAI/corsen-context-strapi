{
  "name": "corsen-context-strapi",
  "version": "1.0.0",
  "private": true,
  "description": "Deployable WebMCP and MCP bridge for public Strapi content.",
  "license": "MIT",
  "author": "Corsen AI",
  "repository": "github:CorsenAI/corsen-context-strapi",
  "homepage": "https://strapi-webmcp.corsen.ai",
  "bugs": "https://github.com/CorsenAI/corsen-context-strapi/issues",
  "keywords": ["webmcp", "mcp", "strapi"],
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "start:env": "node --env-file=.env server.js",
    "test": "node --check server.js && node scripts/test-integration.mjs"
  },
  "dependencies": {
    "@corsenai/corsen-context": "^2.0.1",
    "express": "^5.0.0"
  },
  "overrides": {
    "@corsenai/corsen-context": {
      "fast-xml-parser": "^5.7.0"
    }
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
